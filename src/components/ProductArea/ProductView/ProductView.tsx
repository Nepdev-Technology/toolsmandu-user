import ProductScrollArea from '../ScrollArea/ScrollArea';

const ProductView = () => {
  return (
    <section>
      <ProductScrollArea></ProductScrollArea>
    </section>
  );
};

export default ProductView;
